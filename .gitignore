# Data files (sensitive)
data/private/
data/raw_data/
prepare_data.py
scripts
# But allow scripts needed for CI/CD
!scripts/process_google_form_submissions.py
!scripts/evaluate_all_submissions.py
!scripts/generate_leaderboard.py
!scripts/download_private_data.py

# Google API credentials (NEVER commit these!)
*.json
!package.json
!tsconfig.json
!*.config.json
# But specifically ignore service account keys
*service-account*.json
*credentials*.json
shift-gnn-challenge-*.json
.processed_submissions.json
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db


# Evaluation artifacts (generated in CI, not committed)
evaluation_results.json
.pr_submission_status.json
results/

# Leaderboard data (will be generated, but we need to commit them for GitHub Pages)
# Note: These files are auto-generated but must be in the repo for GitHub Pages to work
# leaderboard.json
# leaderboard.html